import React from 'react';

import AddressScene from '../index';

const FromAddressScene = () => (
  <AddressScene
    messageTitle="Set your address"
    message="We need a from address so your friends can write you back!"
  />
);

export default FromAddressScene;